# Global strategy parameters (can be overridden by CLI args)
timeframe_sec: 1800
rsi_period: 2
oversold: 10.0

take_profits_pts: 4.0
stop_loss_pts: 30.0

size_gbp_per_point: 2.0
only_long: true
one_position_at_a_time: true
dry_run: true
log_level: "INFO"

# Market-specific configurations
markets:
  US500:
    symbol: "US 500"
    epic: "IX.D.SPTRD.DAILY.IP"
    tz: "America/New_York"
    session_open: "09:30"
    session_close: "16:00"
    no_trade_first_minutes: 30
    spread_assumption_pts: 0.6

  # GERMANY40:
  #   symbol: "Germany 40"
  #   epic: "IX.D.DAX.DAILY.IP"
  #   tz: "Europe/Berlin"
  #   session_open: "09:00"
  #   session_close: "17:30"
  #   no_trade_first_minutes: 30
  #   spread_assumption_pts: 2

# Default market (used if --market not specified)
default_market: "US500"
